# Compiled binary
/gateway
/tmp/

# SQLite config store (control plane, later phases)
*.db
*.db-shm
*.db-wal

# Go build/test artifacts
*.out
*.test

# Local env file with real secrets (keep .env.example tracked)
.env
.env.local
!.env.example

# Deployment details (project IDs, secrets, scripts) — kept entirely local
/deploy/
/.gcloudignore

# Editor / OS
.DS_Store
.idea/
.vscode/
