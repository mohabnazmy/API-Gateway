# Compiled binary
/gateway
/tmp/

# SQLite config store (control plane, later phases)
*.db
*.db-shm
*.db-wal

# Go build/test artifacts
*.out
*.test

# Local env file with real secrets (keep .env.example tracked)
.env
.env.local
!.env.example

# Editor / OS
.DS_Store
.idea/
.vscode/
